//
// Created by ishuena on 09.10.17.
//

#include "Vertex.h"
namespace colcod {

 /*   Vertex::Vertex(std::string n, double w) {
        name = n;
        weight = w;
    }

    Vertex::Vertex(std::string n, double w, uint32_t c) {
        name = n;
        weight = w;
        color = c;
    }

    const std::string Vertex::get_name() const {
        return name;
    }

    double Vertex::get_weight() {
        return weight;
    }

    uint32_t Vertex::get_color() {
        return color;
    }

    unsigned long Vertex::get_number_of_neighbors() {
        return neighbors.size();
    }

    Vertex Vertex::get_neighbor(int i) {
        return neighbors.at(i);
    }


    void Vertex::set_neighbor(Vertex v) {
        neighbors.push_back(v);
    }


    bool Vertex::operator==(Vertex const &other) const {
        return name.compare(other.name) == 0;
        //return id == other.id;
    }*/
}